############################################################
#
# cuttle Makefile
# Generated by amyznikov Sep 26, 2016
#   from 'linux-g++-executable' template
#
############################################################

SHELL = /bin/bash

TARGET = protoc-gen-corpc

all: $(TARGET)


cross   =
sysroot =
prefix  = /usr/local
bindir  = $(prefix)/bin
incdir  = $(prefix)/include
libdir  = $(prefix)/lib
DESTDIR =

# DEFINES += -D__STDC_CONSTANT_MACROS -D__STDC_FORMAT_MACROS
INCLUDES+= -I.
SOURCES = $(wildcard *.c *.cc *.cpp *.cxx)
HEADERS = $(wildcard *.h)
MODULES = $(foreach s,$(SOURCES),$(addsuffix .o,$(basename $(s))))


# C preprocessor flags
CPPFLAGS=$(DEFINES) $(INCLUDES)

# C Compiler and flags
CC = $(cross)gcc -std=c11
CFLAGS= -Wall -Wextra -O3 -g3

# C++ Compiler and flags
CXX = $(cross)g++ -std=c++11
CXXFLAGS = $(CFLAGS)


# Loader Flags And Libraries
LD=$(CXX)
LDFLAGS = $(CXXFLAGS)

# STRIP = $(cross)strip --strip-all
STRIP = @echo "don't strip "

LDLIBS += -L/usr/lib -lprotoc -lprotobuf -lstdc++


#########################################


$(MODULES): $(HEADERS) Makefile
$(TARGET) : $(MODULES) Makefile
	$(LD) $(LDFLAGS)  $(MODULES) $(LDLIBS) -o $@

clean:
	$(RM) $(MODULES)

distclean: clean
	$(RM) $(TARGET)

install: $(TARGET) $(DESTDIR)/$(bindir)
	$(STRIP) --strip-all $(TARGET) && cp $(TARGET) $(DESTDIR)/$(bindir)

uninstall:
	$(RM) $(DESTDIR)/$(bindir)/$(TARGET)


$(DESTDIR)/$(bindir):
	mkdir -p $@
